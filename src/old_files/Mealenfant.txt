//import React from 'react';

const Mealenfant = ({meal}) => {
    return (
        <>
        {meal ? (
            <article>
            <h2>{meal.strMeal}</h2>            
        </article>
        ) : (
            <p>Pas de recette/// strMeal vient du tableau json. </p>
        )}
        
        </>
    );
};

export default Mealenfant;